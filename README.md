# oci-identity-domain-user-lister
Compute-native OCI Identity Domains user lister that uses instance principal authentication, discovers accessible domains, selects the best reachable regional domain endpoint by latency, and lists users interactively or non-interactively as table or JSON.
